#!/usr/bin/node
const add = require('./13-add.js').add; // import the function from the other file using the require function and the path to the file
console.log(add(3, 5)); // call the function with two arguments and print the result using console.log
