#!/usr/bin/node

const languages = ['C is fun', 'Python is cool', 'JavaScript is amazing']; // create an array of strings with the three lines
for (let i = 0; i < languages.length; i++) { // use a for loop to iterate over the array
  console.log(languages[i]); // print each element of the array using console.log
}
