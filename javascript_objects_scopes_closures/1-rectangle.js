#!/usr/bin/node

// Define the class Rectangle
class Rectangle {
    // Define the constructor with two parameters w and h
    constructor(w, h) {
        // Initialize the instance attribute width with the value of w
        this.width = w;
        // Initialize the instance attribute height with the value of h
        this.height = h;
    }
}
